apiVersion: v1
kind: Secret
metadata:
  name: {{ include "trivy-operator-dashboard.fullname" . }}
type: Opaque
data:
  DC_CONN_STRING: {{ .Values.history.distributedCache.connectionString | b64enc | quote }}
